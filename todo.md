- [ ] 封装一层抽象, 让 HamuEngine 对外暴露的 API 不要依赖第三方库, 第三方库的头文件全部在 `.cpp` 文件引入。抽象一层 rhi, 简单来说我们需要提供一份通用 API, 例如定义为 Buffer.h, 然后我们例如 Render/OpenGL 目录内部, 做针对 OpenGL 渲染后端的实际实现, 然后 cmake 也方便使用宏配置不同的实现。对外我们只提供 `Buffer.h` 就好了。